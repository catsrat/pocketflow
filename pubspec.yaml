name: pocketflow
description: PocketFlow - Complete Expense Tracker MVP scaffold
publish_to: "none"
version: 0.1.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  sqflite: ^2.2.8+4
  path: ^1.9.3
  provider: ^6.1.4
  intl: ^0.18.0
  csv: ^5.0.0
  fl_chart: ^0.60.0
  share_plus: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
